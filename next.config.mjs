/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  experimental: {
    allowedDevOrigins: ["172.20.10.2", "localhost:3000"],
  },
};

export default nextConfig;
